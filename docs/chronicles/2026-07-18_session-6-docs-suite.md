# Chronicle — Session 6: the documentation suite + the readiness map

**Date:** 2026-07-18 · **Trigger:** Mitch: *"i think this construction is
good, lets hone this into a more neat documentation suite. and inform me what
may be needing completed to give this a good chance at the hack."*

## What was written

- **`README.md`** (root, new) — the front door: plain-words pitch, status
  table (WP0–7 green, Act II + WP8/9 open), quickstart, doc fast-paths,
  layout. Ecosystem-light by design: submissions stay named in
  `render/expressions.yaml` only.
- **`docs/README.md`** (new) — suite index with four reading paths (judge /
  demo-runner / builder / auditor) + a one-job-per-document inventory.
- **`docs/architecture.md`** (new) — the system on one page: layer diagram,
  assessment pipeline (incl. the 1−σ correction), the ceremony and closed
  verdict lexicon, the clock-free ledger, the Lexon correspondence, both
  adapter lanes, the three-verification-lane rationale, and an **honest
  limits** section (in-memory session, proof stubs, float seam, Act II
  unbuilt, σ_mr gap).
- **`docs/demo-runbook.md`** (new) — Act I click-by-click with speak-lines
  (doubles as the video script, ~3 min), Act II outline from plan §3,
  troubleshooting.
- **`docs/hack-readiness.md`** (new) — the gap map read AGAINST
  `expressions.yaml`'s real deadlines (31 July: note + site skeleton + two
  GDC materials · 8 Sept: pooling demo + video), each item with a holder;
  headline risks: **no git history yet**, browser QA never done, and Act II
  two-authority pooling (the registry's own word: the differentiator)
  unbuilt.
- **`docs/chronicles/README.md`** (new) — session index.

No engine, schema, or app code changed; freeze untouched.

## The readiness verdict (summary — full table in hack-readiness.md)

To 31 July: (1) Mitch's gate 1 + FIRST COMMIT/TAG, (2) site skeleton with a
citable URL, (3) concept-note draft in the render lane, (4) one browser QA +
screenshot evening, (5) GDC harvest, (6) deploys — items 1/3-send/6 are
Mitch's; the rest are 2–3 build sessions. To 8 Sept: Act II pooling (multi-
session API → minimised bundle → offline verify → ceiling render → revocation
propagation) + pooling-census freeze + video + demo-grade signatures. The
engine itself needs nothing for the note — remaining work is expression, not
foundation.
